FROM node:24-alpine

WORKDIR /app

ENV NEXT_TELEMETRY_DISABLED=1

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

CMD ["sh", "-c", "if [ -d .next ]; then find .next -mindepth 1 -maxdepth 1 -exec rm -rf {} +; fi && npm run dev -- --hostname 0.0.0.0 --port 3000"]
